{
  "name": "mphelper",
  "version": "2.13.0",
  "private": true,
  "description": "MPHelper – Vendoroo Marketplace WO helper (Tampermonkey + Chrome extension)",
  "scripts": {
    "build": "node scripts/build.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "sharp": "^0.34.5"
  }
}
